name: Coverage Report

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  coverage:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Install llvm-cov
        run: |
          rustup component add llvm-tools-preview
          cargo install cargo-llvm-cov

      - name: Run tests and generate coverage JSON
        run: |
          cargo llvm-cov --json > coverage.json

      - name: Generate Markdown table
        run: python3 coverage_report.py

      - name: Commit and push coverage report
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add COVERAGE.md
          git diff --cached --quiet || git commit -m "Update coverage report"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/2SCloud/2scloud-dns.git HEAD:main
